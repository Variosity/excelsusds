import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { getStripe } from "@/lib/stripe";
export async function GET() { try { const supabase = await createClient(); const { data: auth } = await supabase.auth.getUser(); if (!auth.user) return NextResponse.json({ error: "Please sign in first." }, { status: 401 }); const { data: profile } = await supabase.from("profiles").select("role,stripe_subscription_id,trial_started_at").eq("id", auth.user.id).single(); if (!profile) return NextResponse.json({ error: "Profile unavailable." }, { status: 400 }); if (profile.role === "admin") return NextResponse.json({ access: "owner", plan: null, status: "owner", trialEndsAt: null }); if (!profile.stripe_subscription_id) return NextResponse.json({ access: profile.trial_started_at ? "expired" : "trial_available", plan: null, status: profile.trial_started_at ? "expired" : "inactive", trialEndsAt: null }); const subscription = await getStripe().subscriptions.retrieve(profile.stripe_subscription_id); return NextResponse.json({ access: subscription.status === "trialing" ? "trialing" : ["active", "past_due"].includes(subscription.status) ? "subscriber" : "expired", plan: subscription.metadata.plan_id ?? "paid", status: subscription.status, trialEndsAt: subscription.trial_end ? subscription.trial_end * 1000 : null }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Billing unavailable." }, { status: 500 }); } }
